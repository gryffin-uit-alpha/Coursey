apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: coursey-database
spec:
  selector:
    app: coursey-database
  ports:
  - port: 3306
    targetPort: mysql-port
    protocol: TCP
  type: ClusterIP